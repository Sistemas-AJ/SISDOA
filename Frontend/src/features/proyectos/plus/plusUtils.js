// Funciones utilitarias para las features plus de proyectos

// Ejemplo: normalizar etiquetas
export function normalizarEtiqueta(etiqueta) {
  return etiqueta.trim().toLowerCase();
}

// Otras utilidades pueden ir aquí
